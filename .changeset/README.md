# Changesets

Use `npm run changeset` to create a release note entry.

Typical release flow:

1. Create one or more changesets in pull requests.
2. Merge to `main`.
3. The release workflow opens/updates a "Version Packages" PR.
4. Merge that PR to publish to npm.
